# ── Command-line utilities ───────────────────────────────────────────────────
# Standalone host-side tools built on the project's libraries.  Each tool
# lives in its own subdirectory so more can be added without disturbing the
# others.  These are developer/maintenance utilities, not part of the
# emulator runtime.
# ─────────────────────────────────────────────────────────────────────────────

add_subdirectory(disk)
