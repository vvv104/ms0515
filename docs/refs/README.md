# Документация-первоисточник МС0515

Этот каталог содержит расшифрованные (OCR + ручной вычитка) русские
сканы оригинальной заводской документации на ПЭВМ «Электроника МС 0515»
производства завода «ПРОЦЕССОР» (г. Воронеж).

Источник сканов: каталог `scans/` в архиве пользовательских материалов
(DjVu/PDF от 2011 года, оригиналы — на сайте `www.tis.kz`).

## Расшифрованные документы

| Файл | Документ | Шифр | Объём |
|------|----------|------|-------|
| [`0515-PO.md`](0515-PO.md) | Программы технического обслуживания. Руководство по техническому обслуживанию. | `.00103-01 96 01` | 10 стр. |
| [`0515-FO.md`](0515-FO.md) | Формуляр (паспорт изделия). | `2.791.864 ФО` | 16 стр. |
| [`docs-inventory.md`](docs-inventory.md) | Опись всей известной документации МС-0515 (из `Docs_XLS.rar`). | — | 12 листов XLS |

## Ключевые открытия из этих документов

1. **Расшифровка драйверов** (из приложения 0515-PO):
   - `DZ.SYS` — драйвер гибкого диска
   - `VS.SYS` — драйвер устройства **звуковоспроизведения** (НЕ видео!)
   - `VM.SYS` — драйвер имитатора **дисковода ОЗУ** (RAM-диск)
   - `TT.SYS` — драйвер терминала
   - `MON8SJ.SYS` — монитор системы
   - `SWAP.SYS` — файл свопинга

2. **Расшифровка `183107.SAV/EXE`** — это **«Упражнитель МС0515»**
   (тренировочная/exerciser-программа), главная тестовая программа из
   штатной поставки. Она циклически «упражняет» ЦП, таймер, контроллёр
   НГМД и интерфейс последовательного обмена на стресс-нагрузке.
   Тот же файл найден в архивных дискетах ARCSAV/disk4 под именем
   `183107.EXE`.

3. **Реликвия пользователя**: в формуляре указан заводской № **2.791.864**,
   завод «ПРОЦЕССОР», г. Воронеж, выпуск **октябрь 1992 г.**, исполнение
   **МС 0515.03**. Серийные номера комплектующих:
   - Монитор МС-6113М: 9070-9070
   - Клавиатура МС-7004: 445.95.043315

4. **Минимальный загрузочный набор**: `MON8SJ.SYS` + `SWAP.SYS` + `DZ.SYS`
   + `TT.SYS` — это тот же базовый комплект, что наблюдается в наших
   распаковках (например, на диске Buhgal — только эти и есть).

5. **Программная среда**: ПЭВМ обеспечивает использование ОС **ОСА**,
   совместимой с RT-11/SJ-11 (т.е. это **RT-11-совместимая** Soviet OS),
   плюс языки **АССЕМБЛЕР** и **БЕЙСИК**.

## Ещё не расшифровано

В `scans/` есть ещё крупные архивы которые предстоит обработать:

| Архив | Размер | Вероятное содержание |
|-------|--------|----------------------|
| `mc0515-osa.rar` | 21 МБ | Документация на ОС ОСА |
| `mc0515-po.rar` | 4.5 МБ | Программное обеспечение (расширенное) |
| `mc0515-to.rar` | 37 МБ | Техническое описание |
| `mc0515-ed.rar` | 39 МБ | Эксплуатационная документация |
| `hb1-hb2.rar` | 43 МБ | ? (вероятно, схемы) |
| `hc1-np2.rar` | 2.9 МБ | ? |
| `hc4-to.rar` | 42 МБ | ? |
| `MC1702.rar` + `MC1702-SH.rar` | 30 МБ | МС1702 (видеомодуль/монитор) |
| `mc0585.rar` | 2.1 МБ | МС0585 (видимо родственная модель) |
| `ngmd-nmd.rar` | 2.0 МБ | НГМД/НМД — Гибкий/Магнитный диск |
| `ps-kb.rar` | 2.2 МБ | Блок питания + Клавиатура |
| `Docs_XLS.rar` | 42 КБ | Excel-описи |

Расшифровка крупных архивов потребует больше итераций — каждый файл
будет ~50-200 страниц.
