# ── ms0515_platform_gui unit tests ───────────────────────────────────────────
# Placeholder — the previous attach/release console helpers got cut when the
# GUI binary dropped its console-output flow entirely.  All that's left in
# the gui platform layer are native file dialogs and font candidate paths,
# both of which need user interaction or filesystem inspection to be
# meaningful.  Add tests here when something testable lands.
# ─────────────────────────────────────────────────────────────────────────────
